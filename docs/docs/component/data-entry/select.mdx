---
sidebar_position: 6
title: Select
featured: true
component: true
description: Select component to select value from options.
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Select

A dropdown menu for selecting a value from a list of options. Select component to select value from options.

## When To Use

- A dropdown menu for displaying choices - an elegant alternative to the native `<select>` element
- Utilizing [Radio](/docs/component/radio) is recommended when there are fewer total options (less than 5)

## Import

```jsx
import { TxSelect } from "tx-design-ui";
```

## Examples

### Basic Usage

Basic select usage.

<Tabs>
  <TabItem value="preview" label="Preview" default>
    <iframe
      src="https://storybook-tx-design.pages.dev/iframe.html?viewMode=story&id=tx-design-ui-data-entry-txselect--preview-select"
      width="100%"
      height="250px"
      style={{border: '1px solid #ddd', borderRadius: '8px', background: 'white'}}
    />
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
import { TxSelect } from "tx-design-ui";
import { useState } from "react";

const options = [
  { label: "Option 1", value: "1" },
  { label: "Option 2", value: "2" },
  { label: "Option 3", value: "3" },
];

function App() {
  const [value, setValue] = useState("");

  return <TxSelect options={options} value={value} onChange={val => setValue(val)} placeholder="Select an option" />;
}
```

  </TabItem>
</Tabs>

### Sizes

Select comes in three sizes: `small`, `medium` (default), and `large`.

<Tabs>
  <TabItem value="preview" label="Preview" default>
    <iframe
      src="https://storybook-tx-design.pages.dev/iframe.html?viewMode=story&id=tx-design-ui-data-entry-txselect--size"
      width="100%"
      height="250px"
      style={{border: '1px solid #ddd', borderRadius: '8px', background: 'white'}}
    />
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
<TxSelect options={options} size="small" />
<TxSelect options={options} size="medium" />
<TxSelect options={options} size="large" />
```

  </TabItem>
</Tabs>

### Disabled

Disabled select.

<Tabs>
  <TabItem value="preview" label="Preview" default>
    <iframe
      src="https://storybook-tx-design.pages.dev/iframe.html?viewMode=story&id=tx-design-ui-data-entry-txselect--disabled"
      width="100%"
      height="150px"
      style={{border: '1px solid #ddd', borderRadius: '8px', background: 'white'}}
    />
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
<TxSelect options={options} value="1" disabled />
```

  </TabItem>
</Tabs>

### Status

Add status to Select with `status`, which could be `success`, `warning`, or `error`.

<Tabs>
  <TabItem value="preview" label="Preview" default>
    <iframe
      src="https://storybook-tx-design.pages.dev/iframe.html?viewMode=story&id=tx-design-ui-data-entry-txselect--status"
      width="100%"
      height="250px"
      style={{border: '1px solid #ddd', borderRadius: '8px', background: 'white'}}
    />
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
<TxSelect options={options} status="success" />
<TxSelect options={options} status="warning" />
<TxSelect options={options} status="error" />
```

  </TabItem>
</Tabs>

### Full Width

Make the select take full width of its container.

<Tabs>
  <TabItem value="preview" label="Preview" default>
    <iframe
      src="https://storybook-tx-design.pages.dev/iframe.html?viewMode=story&id=tx-design-ui-data-entry-txselect--full-width"
      width="100%"
      height="150px"
      style={{border: '1px solid #ddd', borderRadius: '8px', background: 'white'}}
    />
  </TabItem>
  <TabItem value="code" label="Code">

```jsx
<TxSelect options={options} fullWidth />
```

  </TabItem>
</Tabs>

### Empty State

When no options are available.

```jsx
<TxSelect options={[]} placeholder="No options" />
```

## API

| Property    | Type                                 | Default              | Description                    |
| ----------- | ------------------------------------ | -------------------- | ------------------------------ |
| options     | `Array<{label: string, value: any}>` | `[]`                 | Data source for select options |
| value       | `any`                                | -                    | Current selected value         |
| onChange    | `function(value, option)`            | -                    | Called when select an option   |
| placeholder | `string`                             | `'Select an option'` | Placeholder text               |
| disabled    | `boolean`                            | `false`              | Whether disabled select        |
| size        | `'small' \| 'medium' \| 'large'`     | `'medium'`           | Size of select                 |
| status      | `'success' \| 'warning' \| 'error'`  | -                    | Set validation status          |
| fullWidth   | `boolean`                            | `false`              | Take full width of container   |

## Notes

- The dropdown automatically closes when clicking outside
- The selected option is highlighted in the dropdown
- Empty state is shown when no options are provided
- Each option should have a unique `value`
- The `label` is what's displayed to the user, while `value` is used internally
